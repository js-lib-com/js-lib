/**
 * User agent model. This package contains user agent abstractions like window, page, cookie, etc. It also provides
 * system input / output dialogs, similar to user agent native dialogs but asynchronous.
 */
$package('js.ua');
